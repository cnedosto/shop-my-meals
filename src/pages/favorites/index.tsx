import { FC } from 'react';
import { Layout } from '../../components/Layout/Layout';

const Favorites: FC = () => {
  return (
    <Layout>
      <h1>Favorites</h1>
    </Layout>
  );
};

export default Favorites;
