import { FC } from 'react';
import { Layout } from '../../components/Layout/Layout';

const NewMeal: FC = () => {
  return (
    <Layout>
      <h1>New Meal</h1>
    </Layout>
  );
};

export default NewMeal;
